# MarFastAPI
Testing project for learning **Python**, **FastAPI** and **pipenv**.

## Start project
1) Install `pipenv`
    ``` bash
    pip install pipenv
    ```
2) Install dependencies
    ```bash
    pipenv install
    ```
3) Start project
    ``` bash 
    uvicorn main:app --reload
    ```